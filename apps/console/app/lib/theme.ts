// =============================================================================
// Client-safe theme helpers (alias).
// -----------------------------------------------------------------------------
// All actual logic lives in `@secretlobby/theme`. This file is preserved as a
// thin alias so existing `~/lib/theme` imports keep working without churn.
// =============================================================================

export * from "@secretlobby/theme";
