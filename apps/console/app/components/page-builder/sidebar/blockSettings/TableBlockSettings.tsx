export function TableBlockSettings() {
  return (
    <div className="text-xs text-theme-muted">
      Edit on the canvas — the contextual toolbar lets you add / remove
      rows and columns and toggle the header row.
    </div>
  );
}
