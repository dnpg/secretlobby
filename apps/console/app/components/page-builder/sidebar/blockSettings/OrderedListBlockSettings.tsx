export function OrderedListBlockSettings() {
  return (
    <div className="text-xs text-theme-muted">
      Edit on the canvas — press Enter for a new item, Backspace at the start
      of an empty item to remove it.
    </div>
  );
}
